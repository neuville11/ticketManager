<nav class="navbar navbar-light bg-primary">
  <a class="navbar-brand" href="#">Ticket Manager</a>
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%= root_path%>">Home</a>
      </li>
      <li class="nav-item  <%= current_user ? 'hidden' : '' %>">
        <a class="nav-link" href="<%= new_user_registration_path %>">Sign up </a>
      </li>
      <li class="nav-item  <%= current_user ? 'hidden' : '' %>">
        <a class="nav-link" href="<%= new_user_session_path %>">Sign in </a>
      </li>
      <li class="nav-item <%= current_user ? '' : 'hidden' %>">
        <%= link_to 'Log out', destroy_user_session_path(current_user),
          method: :delete %>
      </li>
    </ul>
</nav>